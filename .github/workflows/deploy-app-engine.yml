name: Deploy to GCP App Engine

on:
  push:
    branches:
      - main
  workflow_dispatch:

  check_run:
    types: [rerequested, completed]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Set PYTHONPATH
        run: echo "PYTHONPATH=$PWD" >> $GITHUB_ENV

      - name: Run unit tests
        run: |
          pip install pytest
          pytest --maxfail=1 --disable-warnings -q

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GAE_SA_KEY }}

      # ✅ Tell gcloud to use the correct Python
      - name: Force gcloud to use system Python
        run: |
          which python
          export CLOUDSDK_PYTHON=$(which python)
          echo "CLOUDSDK_PYTHON=$(which python)" >> $GITHUB_ENV

      - name: Deploy to App Engine
        if: success()
        run: |
          gcloud info
          gcloud app deploy app.yaml --project ${{ secrets.GCP_PROJECT_ID }} --quiet
